(content)

() - regular parentheses

- создает rout-group
- не добавляет путь в URL
- в скобках указывается имя роута

это позволяет настроить специальный layout, который будет действовать, как корневой layout, который будет доступен всем вложенным страницам
